# {{服务名}} — 程序架构文档

> 📐 本文档侧重**系统结构**（描述性）。开发规范见 [`../specification/{{service}}-规范.md`](../specification/{{service}}-规范.md)。
> 🤖 项目入口：[`../../CLAUDE.md`](../../CLAUDE.md) ｜ 文档索引：[`../reference/docs-index.md`](../reference/docs-index.md)（如有）

---

## 1. 概述

### 🎯 项目使命

**{{一句话定位}}** —— {{2-3 行：解决什么问题、面向什么用户/场景、与后端/外部系统的关系}}。

### 技术栈

| 层次 | 技术 |
|------|------|
| UI 框架 | {{React 18 / Vue 3 / …}} |
| 构建 | {{Vite / Webpack}} |
| 桌面壳（如有） | {{Electron 30}} |
| 状态管理 | {{Context / Zustand / Pinia / Redux}} |
| 终端/图表/富文本等关键库 | {{xterm.js / echarts / …}} |
| 样式 | {{CSS Modules / Tailwind / styled-components}} |
| 国际化 | {{i18next / vue-i18n}} |

### 进程模型（Electron 才需要；纯 Web 删除本节）

```
┌──────────────────────────────────────────────────────────┐
│  Main 进程 (Node.js)   {{main 入口}}                       │
│  ├── 窗口管理 / 生命周期                                    │
│  ├── {{SSH / 文件 I/O / 系统集成}}                         │
│  └── 通过 IPC 暴露能力给 Renderer                          │
├──────────────────────────────────────────────────────────┤
│  Preload (contextBridge)  {{preload 入口}}                 │
│  └── 白名单 API 注入 window.{{api}}                        │
├──────────────────────────────────────────────────────────┤
│  Renderer 进程 (Chromium + {{UI 框架}})  {{renderer 入口}} │
│  └── 全部 UI 与业务视图                                     │
└──────────────────────────────────────────────────────────┘
```

（纯 Web 应用：画"浏览器 SPA ↔ 后端 API/WS"一块即可）

---

## 2. 目录结构（自动生成区域）

> 💡 此区域由 `scripts/update_architecture_{{service}}.py` 自动维护（dum-knowledge-base-build 的 hook 驱动）。
> 描述来源：每个源文件首部注释/JSDoc。高层分组见下方 §2.1。

<!-- AUTO-GENERATED:START -->
<!-- 占位：首次运行 scripts/update_architecture_{{service}}.py 后自动填充 -->
```
(pending first run)
```
<!-- AUTO-GENERATED:END -->

### 2.1 目录速览（人工维护）

```
src/
├── {{components/}}        # {{UI 组件}}
├── {{views|pages/}}       # {{页面/视图}}
├── {{services/}}          # {{后端 API 封装}}
├── {{hooks/|composables/}}# {{复用逻辑}}
├── {{store|contexts/}}    # {{全局状态}}
├── {{types/}}             # {{类型定义}}
└── {{locales/}}           # {{国际化}}
```

（只列重要目录，每个一句话职责，不复制完整树）

---

## 3. 视图与路由架构

{{说明路由方式：React Router / ViewState 枚举条件渲染 / Vue Router。列主要视图及切换关系。}}

| 视图 | 入口组件 | 何时显示 |
|---|---|---|
| {{Dashboard}} | {{...}} | {{...}} |

---

## 4. 状态管理

{{全局状态怎么组织：哪些 store/context、各自管什么、跨组件数据怎么流。}}

| Store / Context | 管理的状态 | 关键消费方 |
|---|---|---|
| {{...}} | {{...}} | {{...}} |

---

## 5. IPC 通信（Electron 才需要；纯 Web 删除本节）

### 5.1 通信模式

{{invoke/handle 请求-响应 + send/on 事件推送，描述用法。}}

### 5.2 IPC 通道清单

| 通道 | 方向 | 用途 | 载荷（字段） |
|---|---|---|---|
| {{ssh:connect}} | renderer→main | {{...}} | {{...}} |

---

## 6. 前端服务层

{{services/ 怎么封装后端调用：统一 api() 封装？各 service 对应哪个后端模块？（"必须走统一封装"是规范，归规范文档）}}

| Service | 对应后端模块 | 主要方法 |
|---|---|---|
| {{hostService}} | {{/host/*}} | {{list / create / …}} |

---

## 7. 组件结构

### 7.1 核心组件

| 组件 | 路径 | 职责 |
|---|---|---|
| {{...}} | {{...}} | {{...}} |

### 7.2 通用控件 / 设计系统（如有）

{{可复用控件库的组织方式。}}

---

## 8. 类型系统

{{核心类型文件在哪、关键领域类型（与后端 DTO 的对应关系）。只列契约，不贴实现。}}

| 类型文件 | 关键类型 |
|---|---|
| {{types/index.ts}} | {{...}} |

---

## 9. 主题与国际化

- **主题**：{{主题怎么定义/切换、CSS 变量体系}}
- **国际化**：{{locales 结构、语言切换、默认语言}}

---

## 10. 数据同步与认证（现状描述）

{{登录流程、token 存哪、刷新机制、与后端的同步策略（轮询/WS/SSE）。}}

---

## 11. 构建与部署

| | |
|---|---|
| 构建命令 | {{npm run build}} |
| 构建产物 | {{dist/ 结构}} |
| 部署/分发 | {{静态托管 / Electron 打包 / 自动更新}} |

（分包/懒加载的**硬规则**归规范文档；这里只描述当前产物结构）

---

## 12. 模块依赖关系图

```
{{ASCII art 或 Mermaid：画高层架构层（视图→hooks→services→IPC/API），不画组件树}}
```
