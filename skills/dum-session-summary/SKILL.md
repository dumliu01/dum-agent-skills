---
name: dum-session-summary
description: Use when the user asks to 总结/记录本次会话的修改 into docs/modify_history/, or wants a 会话交接/续接文档、修改记录、change log to resume work in a new session and to later reconcile (修正) design docs. Triggers - "把这次会话的修改总结成文档"、"生成会话总结/交接文档"、"记录修改历史"、"modify_history"、"方便下次会话继续"。
---

# Dum Session Summary

## Overview

把**一次会话的改动**沉淀成一篇标准结构的 Markdown，存到 `docs/modify_history/`。它只做**一件事**——**把这次会话发生了什么"如实记下来"**，给两类下游消费：

1. **续接（用途①，本技能直接服务）** —— 切换/新开会话时，照这篇就能快速接上：知道做到哪、怎么继续、从哪个文件入手。
2. **后续校对设计文档（用途②，由 [`dum-doc-reconcile`](../dum-doc-reconcile/) 在被显式触发时读取本记录完成）** —— 本技能**不主动 grep 设计文档、不判断哪份文档被带偏、不给修改建议**；这些动作一律推迟到用户/另一会话调用 `dum-doc-reconcile` 时再做。

**核心原则：以 git 为客观事实骨架，用会话上下文补"为什么"。** git 知道**改了什么**；只有会话记得**为什么这么改、试过什么被否决**——后者正是这篇文档最大的价值，也是 git 永远拿不到的。

**边界（重要）**：本技能**不触发任何文档修复/对账动作**。即使发现某份设计文档明显已过时，也只在「改动清单/关键决策」里如实陈述事实，不去开"待校对清单"、不给修改建议、不修改任何 `docs/architecture/` `docs/方案设计/` 下的文件。

## 触发判断

**该触发**：用户说"总结这次会话/把改动记录下来/生成交接文档/记录修改历史/方便下次继续"等；或一段较大的工作刚收尾、准备切换上下文时。

**不该触发**：只是要一条 commit message（用 git）；只想知道"改了哪些文件"（`git status` 即可）；要写面向用户的 release notes（那是产品视角，不是开发续接视角）。

## 工作流（按顺序，跳步要说原因）

### 1. 取数：git 事实 + 会话上下文

- **git 客观面**：取本次会话的 commit（`git log` 自会话起点；起点不确定就问用户，或用"上一篇 modify_history 之后"的提交）+ 未提交改动（`git status` / `git diff --stat`）。这是"改动清单"的事实来源。
- **会话主观面**：从对话里捞出 git 看不到的东西——**关键决策、被否决的备选方案、踩过的坑、未完成项、验证方式**。捞不全就回看本会话再补，不要编。

> **明确不做**：不要 grep `docs/architecture/`、`docs/方案设计/`、`docs/guides/`、`docs/product/` 去搜"哪份设计文档被这次改动带偏"，也不要列出"待校对设计文档"或给"建议怎么改"。这是 [`dum-doc-reconcile`](../dum-doc-reconcile/) 被显式触发时才做的事，**本技能完全不碰**。

### 2. 写文档：套用下方 6 节模板

实打实填，**不留 `[TODO]`/`待补充`** 占位。写不出某节就回第 1 步把信息找齐。文档讲清楚逻辑即可，**不贴大段示例代码**（接口签名、配置项名、commit hash 可以）。

### 3. 落盘

按下方"命名与位置约定"存盘；`docs/modify_history/` 不存在就创建。索引无需手动维护——`docs/README.md` 的知识库脚本会自动收录（见 [scripts/docs/update_docs_kb.py](../../dum-knowledge-base-build/SKILL.md) 的扫描机制）。

落盘后**到此结束**：不要顺手去修 `docs/architecture/` 或 `docs/方案设计/`、不要发起 `dum-doc-reconcile`、不要主动建议用户"现在去对账文档"。等用户后续显式调用 `dum-doc-reconcile` 时，那个技能会读这篇 modify_history 来工作。

## 文档结构模板（6 节，固定顺序）

```markdown
# YYYY-MM-DD 修改摘要：<一句话标题>

## 一句话概述
这次会话到底做成了什么（1–2 句，给"未来的我"看）。

## 背景 / 目标
为什么做、要解决什么问题、触发点是什么。

## 改动清单
按模块/主题分组列改动文件（新增/修改/删除 + 每项一句话作用）。
末尾附本次关键 commit：`<hash> <message>`。

## 关键决策与理由
做了哪些设计选择、为什么；**被否决的方案也要写**（含否决原因）——这是防止下次重走弯路的关键。

## 当前状态 / 验证
已完成到什么程度；怎么验证的（命令/测试/手测结果，给证据不空说）；遗留 TODO 与已知问题。

## 如何续接（下个会话从这接）
从哪里继续、首选下一步、相关入口文件/命令、需要注意的坑。
```

> 不再设"受影响的设计文档（待校对）"一节。改动是否波及哪份设计文档，由后续 `dum-doc-reconcile` 读改动清单与 commit 自行推断；本技能只负责把改动如实记下来，不替它做这步分析。

## 命名与位置约定

- 路径：`docs/modify_history/YYYY-MM-DD-<标题>.md`
- 日期：`YYYY-MM-DD`（用户当前日期，非凭空）。
- 标题：简短描述性短语；空格换 `-`，去掉 `/ \ : * ? " < > |` 等非法字符；可中文。
- 同日多篇：追加 `-2`、`-3`。

## 关键逻辑 / 难点

- **会话边界怎么定**：优先"自本会话开始的全部改动"。无法判定起点时，问用户，或以"上一篇 modify_history 时间点之后"为界——别把历史改动重复计入。
- **取数顺序是 git 先、会话后**：先用 git 锚定确切的文件与 commit（防止漏记/记错），再叠加会话里的理由与取舍。反过来容易凭印象写错文件。
- **职责单一：只记录，不对账**：本技能不负责判断哪份设计文档被本次改动带偏、也不负责修。"是否影响设计文档"这个判断由 `dum-doc-reconcile` 在被显式触发时基于本记录去做——别在这一步抢着做。

## 常见错误

| 错误 | 后果 | 修正 |
|---|---|---|
| 只列"改了什么"，不写"为什么/否决了什么" | 退化成 git log，下次会重走弯路 | 必填"关键决策与理由"，含被否决方案 |
| 顺手 grep `docs/` 找过时设计文档、列"待校对清单"、给"建议怎么改" | 越界做了 `dum-doc-reconcile` 的活，且容易在用户没要求时改动设计文档 | 删掉这类内容；只把改动如实记下来，对账留给后续 reconcile 技能 |
| 落盘后顺手去修 `docs/architecture/` 或 `docs/方案设计/` | 同上，越界且没经用户许可 | 落盘即结束；用户后续要对账时再单独触发 `dum-doc-reconcile` |
| 无"如何续接"或写得太泛 | 用途①失效，新会话接不上 | 写到"改哪个文件、跑什么命令"的颗粒度 |
| 文件名/日期格式各篇不一 | 难以排序与定期回看 | 严格 `YYYY-MM-DD-<标题>.md` |
| 凭印象写文件清单 | 记错/漏记改动 | 以 `git diff --stat` 为准 |
| 留 `[TODO]` 占位 | 半成品文档 | 信息找齐再写，不留草稿语 |

## 出口判断

- ✅ `docs/modify_history/YYYY-MM-DD-<标题>.md` 已生成，6 节齐全、无占位草稿。
- ✅ "改动清单"与 `git diff --stat` 一致；附了本次 commit。
- ✅ "关键决策"含被否决方案。
- ✅ "如何续接"具体到文件/命令颗粒度。
- ✅ `docs/architecture/`、`docs/方案设计/` 下**没有任何文件**因本次"会话总结"被修改；也没替用户去触发 `dum-doc-reconcile`。

漏任一项就还没完成。
