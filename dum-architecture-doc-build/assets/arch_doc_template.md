# {{Service Name}} — 程序架构文档

> 📐 本文档为 Claude Code 开发时的架构参考，与 `CLAUDE.md` 互补。
> `CLAUDE.md` 侧重 **开发规范**，本文档侧重 **系统结构**。
> 📚 知识库总索引（含**全项目文档地图**：哪些文档在哪些目录）：[`./README.md`](./README.md)
> 其它服务的架构文档：[`./{{other_service}}.md`](./{{other_service}}.md)（如有）

---

## 1. 概述

### 🎯 项目使命

**{{一句话定位}}** —— {{2-3 行展开：解决什么问题、面向什么用户/场景、与外部系统的关系}}。

### 技术栈

| 层次 | 技术 |
|------|------|
| {{比如：Web 框架}} | {{比如：FastAPI 0.115 + Uvicorn}} |
| {{...}} | {{...}} |

### 入口 / 进程模型

```
┌────────────────────────────────────────────────────────┐
│           Process: {{进程名}} ({{运行环境}})            │
│  {{入口文件路径}}                                       │
│  ├── {{主要职责 1}}                                     │
│  ├── {{主要职责 2}}                                     │
│  └── {{主要职责 3}}                                     │
└────────────────────────────────────────────────────────┘
```

(如果有多进程，每个进程都画一块 ASCII box；最后说明它们如何协作 — 共享 DB / 走 RPC / event bus 等)

---

## 2. 目录结构（自动生成区域）

> 💡 此区域由 `scripts/update_architecture_{{service}}.py` 自动维护，
> 通过 Claude Code Hook（PostToolUse → Write|Edit）触发更新。
> 描述来源：每个 `.{{ext}}` 文件首部的 {{docstring / 注释}}。
> 高层模块语义见下方 §3 核心模块详解。

<!-- AUTO-GENERATED:START -->
<!-- 占位：首次运行 scripts/update_architecture_{{service}}.py 后自动填充 -->
```
(pending first run)
```
<!-- AUTO-GENERATED:END -->

### 2.1 目录速览（人工维护）

```
{{project_root}}/
├── {{key_dir_1}}/                # {{用途说明}}
│   ├── {{subdir}}/               # {{用途}}
│   └── {{subdir}}/               # {{用途}}
├── {{key_dir_2}}/                # {{用途}}
└── {{key_dir_N}}/
```

(本节是 onboarding 用的高层分组速读，跟 §2 自动生成的详尽清单互补；只列**重要的**目录，每个一句话职责，不要复制完整树形)

---

## 3. 核心模块详解

### 3.1 {{模块 1 名称}} (`{{path/to/module}}`)

**职责**：{{1-2 句}}。

| 子目录 / 关键文件 | 路径 | 职责 |
|---|---|---|
| {{...}} | {{...}} | {{...}} |

(如有阶段演进 / ADR 约束，单独成段说明)

### 3.2 {{模块 2}}

(同上)

### 3.N {{...}}

---

## 4. {{对外接口设计}}

(如果是 API / RPC 服务：列 endpoint、request/response schema、错误码、中间件链；
 如果是 Library：列公开 API + 类型契约；
 如果是 CLI：列子命令；
 如果是前端：列路由 + Pinia store + 后端 API 一对一映射；
 一律通过表格组织，不要列实现细节)

---

## 5. 配置与机密

### 5.1 {{settings 模块}}

| 段 | 关键字段 |
|---|---|
| {{...}} | {{...}} |

### 5.2 {{业务 YAML/JSON 配置}}

(列文件路径 + 关键字段含义，不写完整内容)

---

## 6. 持久化

(数据库 / 文件 / 缓存 / 对象存储 — 每种一段；列表名 + 用途 + 关键迁移)

---

## 7. 测试

| 目录 | 数量 | 性质 |
|---|---|---|
| {{tests/unit/}} | {{...}} | {{...}} |
| {{...}} | {{...}} | {{...}} |

(关键 fixture / CI 配置在哪)

---

## 8. 构建 / 运行

### 8.1 本地开发

```bash
{{install/setup commands}}
{{dev server commands}}
```

### 8.2 部署 / 容器

({{Docker / k8s / systemd / serverless 等}})

### 8.3 测试 / lint 命令

```bash
{{test commands}}
{{lint commands}}
```

---

## 9. 模块依赖图

```
({{ASCII art 或 Mermaid，看你偏好。重点画"高层架构层"，不要画类图——类图属于具体模块的方案文档}})
```

---

## 10. 关键约定与 invariants

1. **{{约定 1 名称}}**：{{1-2 句说明 + 触发场景}}。
2. **{{约定 2 名称}}**：{{...}}。
3. **{{...}}**

(这一节回答的问题：「读完本文档的开发者，写代码前需要知道哪些"不能违反的项目硬规矩"？」例如 PIT 数据约束 / 命名约定 / 注册表自注册 / live trading fail-fast / 棘轮阈值 / merge 流程等)

---

## 11. 当前状态 / 下一步

详见 {{stage history 文档路径}}。最近发布：

- **{{Stage X}}** ({{vX.Y.Z}}) — {{一句话说明}}
- **{{下一站候选}}** — {{...}}
